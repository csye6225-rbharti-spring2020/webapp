Spring Boot Bill Tracking Application.

Build And Deploy:

Prerequisites:

```
JDK 1.8 or later
Maven 3 or later
Spring Boot 2.2.3.RELEASE
```

Instructions:

After cloning the repository:

```
mvn clean install
mvn spring-boot:run
```